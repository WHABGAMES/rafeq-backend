{
  /*
   * ═══════════════════════════════════════════════════════════════════════════
   * ملف إعدادات البناء - tsconfig.build.json
   * ═══════════════════════════════════════════════════════════════════════════
   * 
   * هذا الملف يُستخدم عند بناء المشروع للإنتاج (Production)
   * يرث من tsconfig.json ويستثني بعض المجلدات
   * 
   * الفرق عن tsconfig.json:
   * - يستثني ملفات الاختبار
   * - يستثني مجلد node_modules
   * - يستثني المجلد المترجم (dist)
   * 
   * يُستخدم تلقائياً عند تشغيل: npm run build
   * ═══════════════════════════════════════════════════════════════════════════
   */

  // "extends": وراثة الإعدادات من ملف آخر
  // كل إعدادات tsconfig.json تُطبق هنا + الإعدادات الإضافية
  "extends": "./tsconfig.json",

  // "exclude": استثناء هذه المجلدات من البناء
  "exclude": [
    // مجلد المكتبات - لا نبنيه
    "node_modules",
    
    // ملفات الاختبار - لا نريدها في الإنتاج
    "test",
    
    // مجلد الإخراج - لتجنب الحلقة اللانهائية
    "dist",
    
    // ملفات الـ unit tests
    "**/*spec.ts"
  ]
}
