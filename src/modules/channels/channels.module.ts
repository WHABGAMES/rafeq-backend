/**
 * ╔═══════════════════════════════════════════════════════════════════════════════╗
 * ║              RAFIQ PLATFORM - Channels Module                                  ║
 * ║                                                                                ║
 * ║  📌 الوحدة الرئيسية لجميع قنوات التواصل                                         ║
 * ║                                                                                ║
 * ║  تجمع:                                                                         ║
 * ║  - WhatsApp: الرسائل عبر واتساب                                               ║
 * ║  - Instagram: رسائل إنستقرام المباشرة                                          ║
 * ║  - Discord: بوت الدعم في ديسكورد                                              ║
 * ╚═══════════════════════════════════════════════════════════════════════════════╝
 */

import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { ConfigModule } from '@nestjs/config';

// Sub-modules
import { WhatsAppModule } from './whatsapp/whatsapp.module';
import { InstagramModule } from './instagram/instagram.module';
import { DiscordModule } from './discord/discord.module';
import { TelegramModule } from './telegram/telegram.module';
import { SmsModule } from './sms/sms.module';
import { EmailModule } from './email/email.module';

// Entities
import { Channel } from '@database/entities';

/**
 * ═══════════════════════════════════════════════════════════════════════════════
 * 📌 ما هي القناة (Channel)؟
 * ═══════════════════════════════════════════════════════════════════════════════
 * 
 * القناة = وسيلة تواصل مربوطة بالمتجر
 * 
 * مثال:
 * متجر "الأناقة" لديه:
 * - قناة WhatsApp: +966501234567
 * - قناة Instagram: @alanaga_store
 * - قناة Discord: Alanaga Community Server
 * 
 * كل قناة مستقلة ولها:
 * - إعدادات خاصة
 * - حالة اتصال
 * - credentials (tokens, API keys)
 * 
 * ═══════════════════════════════════════════════════════════════════════════════
 * 📌 Channel Types:
 * ═══════════════════════════════════════════════════════════════════════════════
 * 
 * ┌─────────────────────────────────────────────────────────────────────────────┐
 * │                        SUPPORTED CHANNELS                                   │
 * ├─────────────┬──────────────────────────────────────────────────────────────┤
 * │   Channel   │                      Details                                 │
 * ├─────────────┼──────────────────────────────────────────────────────────────┤
 * │             │ • WhatsApp Business API (Cloud API)                          │
 * │  WhatsApp   │ • يتطلب: Business Account + Phone Number                     │
 * │     📱      │ • الأكثر استخداماً في السعودية                                │
 * │             │ • Rate: 80 msg/sec (Business), 1000/day (Standard)          │
 * ├─────────────┼──────────────────────────────────────────────────────────────┤
 * │             │ • Instagram Messaging API                                    │
 * │  Instagram  │ • يتطلب: Professional/Business Account                       │
 * │     📸      │ • شائع للمتاجر الصغيرة                                        │
 * │             │ • Rate: 200/hour, 1000/day                                   │
 * ├─────────────┼──────────────────────────────────────────────────────────────┤
 * │             │ • Discord Bot API                                            │
 * │   Discord   │ • يتطلب: Discord Application + Bot                           │
 * │     🎮      │ • مثالي للمنتجات الرقمية والألعاب                             │
 * │             │ • Rate: 50 req/sec global                                    │
 * └─────────────┴──────────────────────────────────────────────────────────────┘
 * 
 * ═══════════════════════════════════════════════════════════════════════════════
 * 📌 Channel Status:
 * ═══════════════════════════════════════════════════════════════════════════════
 * 
 * ACTIVE    → القناة متصلة وتعمل
 * INACTIVE  → القناة موقوفة مؤقتاً
 * ERROR     → خطأ في الاتصال (token expired, etc.)
 * PENDING   → في انتظار التحقق
 */

@Module({
  imports: [
    // ═══════════════════════════════════════════════════════════════════════════════
    // 📁 Database
    // ═══════════════════════════════════════════════════════════════════════════════
    TypeOrmModule.forFeature([Channel]),

    // ═══════════════════════════════════════════════════════════════════════════════
    // 📦 Channel Sub-modules
    // ═══════════════════════════════════════════════════════════════════════════════
    /**
     * كل قناة في module منفصل لأن:
     * 1. كل قناة لها API مختلف
     * 2. كل قناة لها rate limits مختلفة
     * 3. كل قناة لها authentication مختلف
     * 4. فصل الكود يسهل الصيانة
     */
    WhatsAppModule,
    InstagramModule,
    DiscordModule,
    TelegramModule,
    SmsModule,
    EmailModule,

    ConfigModule,
  ],

  exports: [
    // نصدّر الـ sub-modules ليمكن استخدام خدماتها
    WhatsAppModule,
    InstagramModule,
    DiscordModule,
    TelegramModule,
    SmsModule,
    EmailModule,
  ],
})
export class ChannelsModule {}

/**
 * ═══════════════════════════════════════════════════════════════════════════════
 * 📌 كيف تعمل القنوات معاً:
 * ═══════════════════════════════════════════════════════════════════════════════
 * 
 * ┌─────────────────────────────────────────────────────────────────────────────┐
 * │                         UNIFIED MESSAGING FLOW                              │
 * ├─────────────────────────────────────────────────────────────────────────────┤
 * │                                                                             │
 * │  ┌──────────┐   ┌──────────┐   ┌──────────┐                                │
 * │  │ WhatsApp │   │Instagram │   │ Discord  │                                │
 * │  │    📱    │   │    📸    │   │    🎮    │                                │
 * │  └────┬─────┘   └────┬─────┘   └────┬─────┘                                │
 * │       │              │              │                                       │
 * │       │   Webhooks   │   Webhooks   │   Gateway                            │
 * │       ▼              ▼              ▼                                       │
 * │  ┌────────────────────────────────────────────────┐                        │
 * │  │              Channels Module                   │ ◄── أنت هنا            │
 * │  │   (Normalize & Route incoming messages)       │                        │
 * │  └───────────────────────┬────────────────────────┘                        │
 * │                          │                                                  │
 * │                          │  Unified Message Format                          │
 * │                          ▼                                                  │
 * │  ┌────────────────────────────────────────────────┐                        │
 * │  │             Messaging Module                   │                        │
 * │  │   (Store, Process, Route to AI/Agent)         │                        │
 * │  └───────────────────────┬────────────────────────┘                        │
 * │                          │                                                  │
 * │            ┌─────────────┴─────────────┐                                   │
 * │            │                           │                                   │
 * │            ▼                           ▼                                   │
 * │     ┌──────────┐               ┌──────────┐                               │
 * │     │    AI    │               │  Human   │                               │
 * │     │  Module  │               │  Agent   │                               │
 * │     └────┬─────┘               └────┬─────┘                               │
 * │          │                          │                                      │
 * │          │     Reply                │     Reply                           │
 * │          └──────────┬───────────────┘                                      │
 * │                     │                                                       │
 * │                     ▼                                                       │
 * │  ┌────────────────────────────────────────────────┐                        │
 * │  │              Channels Module                   │                        │
 * │  │   (Route outgoing message to correct channel) │                        │
 * │  └───────────────────────┬────────────────────────┘                        │
 * │                          │                                                  │
 * │       ┌──────────────────┼──────────────────┐                              │
 * │       │                  │                  │                              │
 * │       ▼                  ▼                  ▼                              │
 * │  ┌──────────┐      ┌──────────┐      ┌──────────┐                         │
 * │  │ WhatsApp │      │Instagram │      │ Discord  │                         │
 * │  │   API    │      │   API    │      │   API    │                         │
 * │  └──────────┘      └──────────┘      └──────────┘                         │
 * │                                                                             │
 * └─────────────────────────────────────────────────────────────────────────────┘
 * 
 * ═══════════════════════════════════════════════════════════════════════════════
 * 📌 Unified Message Format:
 * ═══════════════════════════════════════════════════════════════════════════════
 * 
 * بغض النظر عن القناة، الرسالة تُحوّل لصيغة موحدة:
 * 
 * {
 *   channel: 'whatsapp' | 'instagram' | 'discord',
 *   channelId: 'uuid',
 *   externalMessageId: 'msg_xxx',      // ID من القناة الأصلية
 *   
 *   sender: {
 *     externalId: '966501234567',      // ID في القناة
 *     name: 'أحمد محمد',
 *     phone: '+966501234567',          // إذا متاح
 *   },
 *   
 *   content: {
 *     type: 'text' | 'image' | 'audio' | ...,
 *     text: 'مرحبا، أين طلبي؟',
 *     mediaUrl: null,
 *   },
 *   
 *   timestamp: '2024-01-15T10:30:00Z',
 *   metadata: { ... }                   // بيانات خاصة بالقناة
 * }
 */
